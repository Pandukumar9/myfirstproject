import { CustamPipesPipe } from './custam-pipes.pipe';

describe('CustamPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CustamPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
