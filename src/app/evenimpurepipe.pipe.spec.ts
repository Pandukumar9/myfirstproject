import { EvenimpurepipePipe } from './evenimpurepipe.pipe';

describe('EvenimpurepipePipe', () => {
  it('create an instance', () => {
    const pipe = new EvenimpurepipePipe();
    expect(pipe).toBeTruthy();
  });
});
